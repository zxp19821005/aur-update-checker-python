# 核心依赖
PySide6>=6.9.1  # Qt框架，用于构建用户界面
requests>=2.32.4  # HTTP请求库，用于与AUR和上游源交互
SQLite-utils>=3.38  # SQLite数据库工具，用于本地存储软件包信息
packaging>=25.0  # 版本解析工具，用于处理软件包版本号
loguru>=0.7.3  # 日志管理库，提供友好的日志输出
qasync>=0.27.1  # 异步事件循环集成库，用于Qt与asyncio的兼容
playwright>=1.54.0  # 现代浏览器自动化工具，用于复杂的上游源检查
beautifulsoup4>=4.13.4  # HTML解析库，用于从网页中提取版本信息
lxml>=6.0.0  # XML解析库，提供快速的HTML/XML处理能力

# 安全工具
safety>=3.6.0  # 用于检查Python依赖中的已知安全漏洞